# ls 
## Overview
- ディレクトリの中身を見る
## Options
- -a ( . )から始まるfile,Dir表示( . )( .. ) も表示する
- -A（ . ）から始まるDirやfileを表示、( . )( .. )は表示しない
- -１ 縦に並べて表示
- -F ファイルの種類も表示
- -d ディレクトリ自身の情報も表示
- -i iノード番号も表示
- -l ファイルの詳細情報表示
- -t 日付順に表示

